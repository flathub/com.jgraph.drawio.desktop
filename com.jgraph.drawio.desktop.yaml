app-id: com.jgraph.drawio.desktop
runtime: org.freedesktop.Platform
runtime-version: '20.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: '20.08'
separate-locales: false
command: start-drawio
rename-desktop-file: drawio.desktop
rename-icon: drawio
finish-args:
  - --share=ipc
  - --socket=x11
  - --share=network
  - --filesystem=home
modules:
  - shared-modules/libsecret/libsecret.json

  - name: drawio
    buildsystem: simple
    build-commands:
      - ar -x *.deb
      - tar -xf data.tar.xz
      - cp -a opt/* usr/* $FLATPAK_DEST
      - rm -rf $FLATPAK_DEST/share/icons/hicolor/{720x720,1024x1024}/
      - mv $FLATPAK_DEST/share/mime/packages/{drawio,$FLATPAK_ID}.xml
      - install -Dm755 start-drawio.sh $FLATPAK_DEST/bin/start-drawio
      - install -Dm644 -t $FLATPAK_DEST/share/metainfo $FLATPAK_ID.metainfo.xml
      - desktop-file-edit --set-key=Exec --set-value="start-drawio %U" $FLATPAK_DEST/share/applications/drawio.desktop
    sources:
      - type: file
        url: https://github.com/jgraph/drawio-desktop/releases/download/v14.5.1/drawio-amd64-14.5.1.deb
        sha256: 0cabdf1e9a051fc38672a9233b1b2d5368d2a776fe3d3f5e09a823278b899976
        x-checker-data:
          type: json
          url: https://api.github.com/repos/jgraph/drawio-desktop/releases/latest
          version-query: .tag_name | sub("^v"; "")
          url-query: .assets[] | select(.name=="drawio-amd64-\($version).deb") | .browser_download_url
      - type: script
        dest-filename: start-drawio.sh
        commands:
          - export TMPDIR="$XDG_RUNTIME_DIR/app/$FLATPAK_ID"
          - exec zypak-wrapper /app/draw.io/drawio "$@"
      - type: file
        path: com.jgraph.drawio.desktop.metainfo.xml
